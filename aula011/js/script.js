// alert("script debugado"); // método ou função do navegador pertencente ao objeto window (janela do navegador)

/**
 * window.alert() // um alerta
 * window.confirm() // retorna um Boolean
 * window.prompt() // formulario
 */

// dentro de um objeto é chamado de método e fora é chamado de função

const numero1 = Number(prompt("digite um valor"));
const numero2 = Number(prompt("digite um valor"));
alert(`${numero1} + ${numero2} = ${numero1 + numero2}`);
